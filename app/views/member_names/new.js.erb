$("#new").html("")
$("#new").html("<%= escape_javascript(render 'new') %>")

$(function($){
  $("#new_member_name").validationEngine();
  $('.input-group.date').datepicker({
    language: 'ja',
    format: "yyyy-mm-dd",
    autoclose: true,
    todayHighlight: true,
    todayBtn: "linked"
  });
});